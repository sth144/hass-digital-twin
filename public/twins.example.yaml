# Starting catalog, copied to twins.yaml on first build. twins.yaml itself is gitignored
# because it names real houses; add private twins there (or via scripts/publish-twin.mjs).
twins:
  - id: playground
    name: Playground
    mapping: /hass_digital_twin/garage.yaml
  - id: gallery
    name: Device model gallery
    mapping: /hass_digital_twin/gallery.yaml
